name: CI of pistop-customer microservice
#Déclencher le pipline si une modification fait dans le pistop-customer microserv
on:
  push:
    path:
      - 'pistop-customer/**'
  pull_request:
    path:
      - 'pistop-customer/**'


jobs:
  build:
    # Ensure the runner is using the latest Ubuntu version

    runs-on: ubuntu-latest  

    steps:
      - name: "Checkout the code to the runner"
        uses: actions/checkout@v4

      - name: "Build Docker image"
        run: |
          ls -al
          cd ./pistop-customer  
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/pistop-customer .  
      - name: "Login to Docker Hub"
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: "Push the image to Docker Hub"
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/pistop-customer
